CommandLineCore, ~> 1.2
ObjectMapper, ~> 3.4

// Submodule name (comma) (space) <test> (space) version
// Tests:
//  == (equal),
//  >= (greater than or equal),
//  ~> (compatible, equal or sub-version)
